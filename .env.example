DISCORD_TOKEN=your-bot-token-here

# Optional: set this while developing so slash commands appear instantly
# in one server instead of waiting for Discord's global command cache.
DISCORD_GUILD_ID=your-server-id-here
# CONNECTION_STRING=